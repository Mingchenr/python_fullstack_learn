1. Python + Django 环境配置（终端 / CMD 执行）
  # 1. 检查python版本
  python --version  #  windows
  
  python3 --version # Mac /Linux
  
  # 2. 安装Django +DRF
  pip install django   djangorestframework
  
  
  # 3. 升级pip
  pip install --upgrade pip
  
  # 4.验证Django安装
  django -admin  --version
  
  # 5.创建第一个Django项目
  django-admin startproject myfirstproject
  cd myfirstproject
  
  # 6.启动Django服务
  python mange.py runserver
  
  #7.浏览器访问http://127.0.0.1：8000/
2. MySQL 环境配置
  方式 1：本地安装（推荐）
Windows：下载 MySQL Installer（https://dev.mysql.com/downloads/installer/），选择「Developer Default」安装，记住 root 密码；
Mac：brew install mysql（需先装 Homebrew），启动：brew services start mysql；
验证：终端输入mysql -u root -p，输入密码能登录即为成功。
  方式 2：使用免费远程 MySQL（备用，避免本地安装麻烦）
推荐平台：阿里云 / 腾讯云免费数据库、PlanetScale（https://planetscale.com/）；
记录：数据库地址、端口、用户名、密码（后续 Django 配置用）。

3. Vue 环境配置（终端 / CMD 执行）
  # 1. 安装Node.js（下载：https://nodejs.org/，选择LTS版本）
  # 验证安装
  node --version
  npm --version
  
  # 2. 安装Vue CLI（全局安装）
  npm install -g @vue/cli
  
  # 3. 验证Vue CLI
  vue --version  # 输出5.x即为成功
  
  # 4. 创建第一个Vue项目
  vue create myfirstvue
  
  # 5. 选择「Default ([Vue 3] babel, eslint)」，等待安装
  cd myfirstvue
  
  # 6. 启动Vue服务
  npm run serve
  
  # 7. 浏览器访问 http://localhost:8080/，看到Vue默认页面即为成功
4. 配置 Django 连接 MySQL
  # 安装MySQL驱动
  pip install mysqlclient  # Windows可能需要装Visual C++，替代方案：pip install pymysql
  
  修改 Django 项目settings.py：
  python
  运行
  # 替换默认的SQLite配置
  DATABASES = {
      'default': {
          'ENGINE': 'django.db.backends.mysql',
          'NAME': 'myfirstdb',  # 需先在MySQL创建数据库
          'USER': 'root',
          'PASSWORD': '你的MySQL密码',
          'HOST': '127.0.0.1',
          'PORT': '3306',
      }
  }

